# 交接：学校资料库 1.0

更新时间：2026-08-18

## 当前状态

`index.html` 已完成从经营看板到最小学校资料库的调整。页面只有学校概况、资料清单和缺失资料；旧经营指标、漏斗、家长分层与提分案例已移除。

关键词搜索、五类筛选和演示原文链接均已实现。2026-08-18 使用 Chrome 1440×1000 验证：10 条资料可正常检索，分类筛选与空结果提示正常，4 项缺失资料直接可见，无横向溢出或脚本异常。验收图见 `artifacts/school-archive-desktop.png`。

## 数据维护

演示资料位于 `index.html` 底部的 `documents` 数组。每条记录只维护 `title`、`category`、`scope`、`updated`、`owner`、`status`、`url` 七个字段。

分类值只能使用：学校信息、教学进度、试卷资料、家长与情报、活动与产品。当前 `https://example.com/` 是明确标记的演示地址，接入真实资料时直接替换对应 `url`。

## 1.0边界

不加入录入编辑、上传、Excel、持久化、后台、权限、多页面、AI、预警、成熟度评分、知识图谱或多校对比；出现真实需求后再单独评估。
