# 乐读内部资料库运维交接

更新日期：2026-09-09

## 生产未切换

生产仍为 `4310e4b9` / `6e2ff6f` / D1 `0007`。上轮交接记录：新工作器计划任务每分钟触发、IgnoreNew、Ready，共享 profile 登录已恢复；“杨老师的陪跑日记”小红书号 `9522680303` 核验成功，检索读取20条候选、打开6条详情、保存5条结果。本轮没有重新执行生产验收或改动上述运行环境。

`ADMIN_KEY` 已于2026-09-02轮换；本轮没有读取或修改任何生产秘密。旧 `Ledu-Xiaohongshu-Course-Trial` 继续禁用但未删除；旧状态、`seen.json`、`held_candidates` 和旧 profile 未读写。

## 本轮源码

- 增加每用户扫码绑定、随机独立 profile、短时加密二维码、解绑/刷新/重新绑定、失效与安全阻断状态。
- 三类工作保持全局串行；认领返回服务端用户和 profile 标识，排除已禁用用户。旧工作器不具备 `user_sessions` 协议，返回409，不能继续使用共享 profile。
- 已排队的资料任务等待绑定，运行中的资料任务阻止更换 profile。刷新使旧回传失效，但保留旧租约直到浏览器关闭或租约结束，不允许并行接管。
- 旧24位界面：用户确认刷新后恢复小红书号表单。未发现仓库第二份表单，未添加自动刷新修补。未逐一枚举其他预览域名。
- 删除已退役七日采集/PDF/上传/AI/恢复/注册代码和相关测试；没有删除实际计划任务或历史数据。保留工作器依赖的浏览器、解析、通知、DPAPI及旧数据展示兼容路径。

## 已完成验证

Node 8+9+16=33项；Python 3+15=18项；Pages Functions构建；全新本地0001～0008迁移均通过。测试覆盖两用户、加密取图、过期、刷新旧回传、解绑保留历史、禁用、排队、旧工作器拒绝和安全停机；Python使用临时合成目录验证清理不影响另一用户和旧共享目录。

GPT内置浏览器完成本地模拟绑定成功、重新绑定、键盘解绑、桌面与390px主导航；横向无溢出，控制台无错误，桌面截图已更新。未进行真实扫码、真实小红书检索或真实AI；其他浏览器的真实扫码仍属上线验收。

## 下次经授权发布

1. 先确认无运行中的任务，停止计划任务触发，保留全部旧数据与凭据；此步骤须单独授权。
2. 备份/核对后应用远端0008并部署新Pages，再把工作器切换到同一源码；生产操作均须单独授权。旧版工作器会被协议检查拒绝，不能靠旧共享profile继续运行。
3. 每个内部用户首次扫码建立自己的profile，不从旧profile复制Cookie。至少两个用户完成真实扫码及各自核验/检索，确认隔离与结果归属。
4. 明确授权后恢复每分钟计划任务。安全阻断只由人工 `repair-login --profile-id <内部随机标识>` 处理并恢复；该命令同样受本机互斥锁保护。

生产D1、Pages部署、任务计划、真实扫码验收和远端删除未获得本轮授权，未执行。仅推送源码不代表已经上线。
