# 交接：两人资料库部署与真实上传

更新时间：2026-08-20

## 当前基线

仓库 `main` 当前提交为 `9ad8b8a`。`index.html` 是可直接打开的单文件静态演示页，已有学校概况、五类资料、关键词搜索、分类筛选、确认状态、缺失资料和响应式布局；没有服务端、数据库、真实文件或部署配置。

本轮只完成方案定稿和文档交接，尚未修改 `index.html`，也尚未创建 Cloudflare 账号或部署环境。

## 已确认需求

- 第一版严格单学校，保留现有五类资料：学校信息、教学进度、试卷资料、家长与情报、活动与产品。
- 使用者固定为“人员1（我）”和“人员2（上级）”。
- 不注册、不登录，两人拥有完全相同的查看、上传、下载、修改状态和删除权限。
- 使用 Cloudflare 自动分配的临时地址，暂不申请公司子域名，不办理 ICP 备案。
- 上传格式仅 PDF、DOCX、XLSX；不支持旧版 XLS 或带宏格式；单文件最大 50MB。
- 上传时手动选择“人员1（我）”或“人员2（上级）”，上传者对两人可见。
- 新上传资料默认为“待确认”；任一人都可改为“已确认”或改回“待确认”。
- 保留搜索、分类筛选、缺失资料和空结果提示。

## 最小架构

- Cloudflare Pages：托管现有原生前端并提供 `*.pages.dev` 临时地址，无构建命令。
- Pages Functions：提供资料列表、上传、下载、状态修改和删除 API。
- R2 私有桶：保存真实文件；对象键使用随机 UUID，不使用原始文件名作为路径。
- D1：保存资料元数据；不依赖短期平台日志作为业务记录。
- 不使用 Cloudflare Access、第三方身份服务、手机号、邮件验证码或前端框架。

预计两人、10GB 以内可使用免费额度；实际以 Cloudflare 控制台当期额度为准。普通 Cloudflare 网络可从中国大陆访问，但跨境上传和下载的速度、稳定性没有大陆节点保证。

## 免登录访问方式

创建一个至少 32 字节随机管理密钥，作为 Cloudflare secret 保存，绝不写入仓库。给两人共享一次带密钥的管理链接；前端保存密钥并从地址栏清除，之后在 API 请求头中携带。

静态页面本身可以加载，但没有正确密钥时，服务端必须拒绝资料列表、上传、下载、修改和删除。密钥泄露时只需在 Cloudflare 更换 secret 并重新发送链接。

这是“免登录共享密钥”，不是身份认证：上传者字段由用户手动选择，可被另一人冒用。第一版明确接受这个取舍，不能把它描述成可核验审计。

## 最小数据与接口

每条资料至少保存：

- `id`
- `title`
- `category`
- `scope`
- `status`
- `uploaded_by`
- `uploaded_at`
- `original_name`
- `object_key`
- `mime_type`
- `size_bytes`
- `deleted_at`（页面删除时写入，默认空）

页面显示七列：标题、分类、年级/学科、上传时间、上传者、确认状态、操作。

API 只需：

- `GET /api/documents`
- `POST /api/documents`
- `GET /api/documents/:id/file`
- `PATCH /api/documents/:id`
- `DELETE /api/documents/:id`

不要创建通用仓储层、用户表、角色表或未来扩展接口。

## 安全与一致性底线

- 所有资料 API（包括列表、写操作和文件下载）都在服务端校验共享密钥。
- 服务端校验文件大小、白名单扩展名、声明 MIME 和基础文件头；不能只信浏览器字段。
- 下载响应使用原文件名、`Content-Disposition: attachment` 和 `X-Content-Type-Options: nosniff`。
- 文件先写 R2，再写 D1；D1 写入失败时删除刚写入的对象。
- 删除前由页面二次确认；第一版只软删除 D1 记录并保留私有 R2 对象，避免跨服务失败造成文件丢失。替换文件按新记录上传，不覆盖原对象。
- 用户输入渲染必须使用安全 DOM/`textContent`，不得把上传字段直接插入 `innerHTML`。
- 不实现自动病毒扫描。理由是只有两名可信使用者；如以后开放给更多人，必须重新评估登录、身份审计和恶意文件扫描。

## 实施顺序

1. 在 `index.html` 增加上传表单、上传者选择、状态操作和错误/进度提示。
2. 将演示 `documents` 数组替换为 API 数据；保留现有五类筛选、搜索和缺失资料。
3. 增加最少的 Pages Functions、D1 建表 SQL 和 Cloudflare 配置说明。
4. 为所有 API 加共享密钥校验和文件边界校验。
5. 本地验证前端与 API，再创建 Cloudflare Pages、D1、R2 和 secret。
6. 部署到临时地址，使用两份小型测试文件完成上传、状态修改、下载和删除闭环。
7. 使用 Edge 或 Chrome 做桌面端验收，更新截图和三份文档，提交并推送 `main`。

## 验收标准

- 两人拿到管理链接后无需登录即可使用全部功能。
- 无密钥或错误密钥不能访问任何真实资料能力。
- 上传者下拉框只有“人员1（我）”和“人员2（上级）”。
- PDF、DOCX、XLSX 可上传；错误格式和超过 50MB 的文件被服务端拒绝并显示明确错误。
- 上传后立即出现在列表中且为“待确认”，上传者和服务端时间正确显示。
- 两人都可修改状态、下载和删除；刷新页面后数据仍存在。
- 搜索、五类筛选、缺失资料、移动端布局和键盘焦点继续正常。
- 浏览器控制台无脚本错误，资料字段不能触发存储型 XSS。

## 需要用户完成的外部步骤

实施阶段需要用户创建或登录 Cloudflare 账号，并在需要时完成控制台授权或计费验证。不要让用户把 Cloudflare API Token、管理密钥或其他秘密粘贴进聊天或提交到 GitHub。
