# 乐读内部资料库运维交接

更新日期：2026-09-09

## 生产已发布，待真实扫码验收

经用户“全部授权”，生产已更新为 `cfa1f6f1` / `aa4f3a6` / D1 `0008`。正式域名返回200，包含扫码绑定和小红书号表单，无旧24位表单。工作器目录 `D:/Project/ledu_project/Intelligence System` 已切换至同一提交，未跟踪 `.codex-tmp/` 原样保留。新版手动空闲轮询返回0；`Ledu-Network-Materials-Worker` 当前Disabled，等待真实扫码验收后恢复既有每分钟、IgnoreNew设置。

迁移前确认无排队/运行中的核验或检索，全局halted=0。迁移前后users=2、accounts=1、jobs=5、results=5，外键检查无异常，新bindings=0。迁移前D1 Time Travel书签：`00000084-0000000a-000050e1-9efe5387e8321692d874f5406a603863`；未导出含会话信息的数据库文件。

历史共享profile验收：“杨老师的陪跑日记”小红书号9522680303，20条候选、6条详情、5条结果；这不是新版两用户验收结果。

`ADMIN_KEY` 已于2026-09-02轮换；本轮没有读取或修改任何生产秘密。旧 `Ledu-Xiaohongshu-Course-Trial` 继续禁用但未删除；旧状态、`seen.json`、`held_candidates` 和旧 profile 未读写。

## 本轮源码

- 增加每用户扫码绑定、随机独立 profile、短时加密二维码、解绑/刷新/重新绑定、失效与安全阻断状态。
- 三类工作保持全局串行；认领返回服务端用户和 profile 标识，排除已禁用用户。旧工作器不具备 `user_sessions` 协议，返回409，不能继续使用共享 profile。
- 已排队的资料任务等待绑定，运行中的资料任务阻止更换 profile。刷新使旧回传失效，但保留旧租约直到浏览器关闭或租约结束，不允许并行接管。
- 旧24位界面：用户确认刷新后恢复小红书号表单。未发现仓库第二份表单，未添加自动刷新修补。未逐一枚举其他预览域名。
- 删除已退役七日采集/PDF/上传/AI/恢复/注册代码和相关测试；没有删除实际计划任务或历史数据。保留工作器依赖的浏览器、解析、通知、DPAPI及旧数据展示兼容路径。

## 已完成验证

Node 8+9+16=33项；Python 3+15=18项；Pages Functions构建；全新本地0001～0008迁移均通过。测试覆盖两用户、加密取图、过期、刷新旧回传、解绑保留历史、禁用、排队、旧工作器拒绝和安全停机；Python使用临时合成目录验证清理不影响另一用户和旧共享目录。

GPT内置浏览器完成本地模拟绑定成功、重新绑定、键盘解绑、桌面与390px主导航；横向无溢出，控制台无错误，桌面截图已更新。未进行真实扫码、真实小红书检索或真实AI；其他浏览器的真实扫码仍属上线验收。

## 剩余真实验收与恢复

1. 每个内部用户首次扫码建立自己的profile，不从旧profile复制Cookie。至少两个用户完成真实扫码及各自核验/检索，确认隔离与结果归属。已请用户在正式站点点击扫码绑定并保持页面打开，随后手动运行新版工作器生成二维码。内置浏览器正式页面控制连续超时，尚未完成生产交互验收。
2. 验收通过后恢复既有每分钟计划任务并核对Ready、IgnoreNew及空闲轮询。无需重新注册任务。
3. 安全阻断只由人工 `repair-login --profile-id <内部随机标识>` 处理并显式恢复；该命令同样受本机互斥锁保护。

本轮迁移、部署、工作器切换、真实扫码/检索验收及任务恢复已获授权，无需重复询问。真实扫码需用户操作手机，尚未完成；远端删除、真实AI和Secret变更未执行。
