# AGENTS.md

## 开始前

完整阅读 `readme.md`、`handoff.md` 和相关代码；确认位于 `main` 并用快进方式同步 `origin/main`，禁止强推。

## 实施边界

- 保持单个 `index.html` 和原生HTML、CSS、JavaScript，不增加依赖或构建工具。
- 页面只保留学校概况、资料清单、缺失资料；资料分类和七个记录字段以 `handoff.md` 为准。
- 演示数据只放在JavaScript数组中并保持匿名；不加入录入、上传、Excel、持久化、后台、权限、AI、多校或预留抽象。
- 只改任务需要的内容，优先删除过期代码。

## 验证与发布

使用电脑端 Edge 或 Chrome 验证搜索、分类筛选、原文链接、缺失资料可见性、布局和脚本错误，并更新 `artifacts/` 中的当前截图。

完成后精简同步三份文档，只暂存本次文件；在本地 `main` 创建简洁提交并直接推送 `origin/main`。遇到冲突、认证失败或未知远端变更时停止，不创建分支或PR，不强推，不创建空提交。
