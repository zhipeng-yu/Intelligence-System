# AGENTS.md

## 开始前

完整阅读 `readme.md`、`handoff.md` 和相关代码；确认位于 `main` 并用快进方式同步 `origin/main`，禁止强推。

## 当前任务

把现有单校资料索引升级为可部署的两人资料库。最终需求与验收标准以 `handoff.md` 为准；开始实施前不要再次扩展需求。

## 实施边界

- 保留单个 `index.html` 和原生 HTML、CSS、JavaScript，不引入前端框架。
- 只增加 Cloudflare Pages Functions、R2、D1 和部署所必需的少量服务端文件；不增加无关依赖、构建体系或预留抽象。
- 使用 Cloudflare 临时域名，不申请公司域名或 ICP 备案。
- 只有“人员1（我）”和“人员2（上级）”两个显示身份；不做注册、登录、手机号、部门或角色系统，两人权限完全相同。
- 使用随机共享管理密钥保护资料 API；前端不显示登录页。上传者由用户手动选择，因此只表示声明身份，不是可信审计身份。
- 仅允许 PDF、DOCX、XLSX，单文件不超过 50MB；对象私有、随机命名、强制附件下载。
- 保留现有五类资料、搜索、筛选、缺失资料和单学校范围；不加入 AI、多校、全文解析、通知或复杂后台。
- 只改任务需要的内容，优先删除被真实 API 取代的演示代码。

## 验证与发布

- 使用电脑端 Edge 或 Chrome 验证资料读取、搜索、分类、上传、下载、状态修改、删除确认、空状态、错误提示、响应式布局和脚本错误。
- 验证无管理密钥时不能读取、上传、下载、修改或删除资料；验证格式和 50MB 限制由服务端执行。
- 更新 `artifacts/` 中的当前桌面端截图，并精简同步 `readme.md`、`handoff.md`、`AGENTS.md`。
- 只暂存本次文件；在本地 `main` 创建简洁提交并直接推送 `origin/main`。遇到冲突、认证失败或未知远端变更时停止，不创建分支或 PR，不强推，不创建空提交。
