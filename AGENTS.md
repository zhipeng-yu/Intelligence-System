# AGENTS.md

本文件约束后续开发代理在本目录内的工作方式。

## 开始前

1. 先完整阅读 `readme.md`、`handoff.md` 和相关代码。
2. 明确用户要求是修改演示看板，还是进入Excel、真实数据或多校阶段；不要自行扩大范围。
3. 确认当前位于 `main` 分支，并先同步远端状态；不要强制覆盖远端历史。

## 当前架构

- `index.html` 是唯一运行文件，包含HTML、CSS、JavaScript和演示数据。
- 页面不需要安装依赖、启动服务或执行构建命令。
- 目标运行环境是电脑端最新版Edge或Chrome。
- `artifacts/` 只保存验收截图等生成物。

除非用户明确要求，不要引入前端框架、包管理器、后端、数据库、账号系统、Excel解析库或多文件工程结构。

## 产品边界

- 当前是“目标学校A × 八年级 × 物理”的单校管理看板。
- 首页只保留五类内容：核心指标、渗透漏斗、L3–L5家长节点、学校档案和提分案例。
- “多校对比”保持未启用，直到至少存在两所真实目标学校。
- 数据仍为演示数据；页面顶部和页脚必须明确标注。
- 不要求手机端适配，不要为移动端增加复杂实现。

## 数据规则

修改数据时必须保持以下关系：

```text
长期班在读渗透率 = 长期班在读学生 / 目标年级学生
目标学校学员渗透率 = 跨活动、短班、长期班去重学生 / 目标年级学生
加微率 = 已加微信去重家长 / 已注册去重家长
核心家长30日活跃率 = 近30日活跃L3–L5家长 / L3–L5家长
```

- 学生人数与家长人数不得混作同一分子或分母。
- 同一学生参加多个项目时只计1名去重学员，同时保留项目标签。
- L3：提供有效学校信息；L4：参与资料、产品或活动共创；L5：主动分享或产生有效转介绍。
- 家长层级升级必须有最近一次行为证据，不接受纯主观标记。
- 有效交流不含问候、群发或无业务价值的聊天。
- 提分案例必须匿名化，且两次考试同学科、同满分制、难度相近，默认至少提升10分。
- 档案完整度按六项计算：目标年级规模、教学进度与教辅、考试日历、历次试卷、升学信息、核心家长与服务记录。

## 修改原则

- 使用中文界面文案，保留现有简洁、克制的视觉方向。
- 只修改与请求直接相关的内容，不顺手重构或格式化整份文件。
- 优先使用浏览器原生HTML、CSS和JavaScript。
- 不写入真实学校、学生、家长姓名、微信号或交流原文；未获授权的信息必须匿名化。
- 新增指标时同时补充名称、公式、数据来源、负责人和更新频率；信息不完整时先标记“待补充”，不要编造。
- 口径或完成状态变化后同步更新 `readme.md` 和 `handoff.md`。

## 最低验证要求

每次修改 `index.html` 后至少完成：

1. 使用Edge或Chrome打开页面，检查目标电脑宽度下无重叠、截断或不可读内容。
2. 检查浏览器控制台没有页面脚本错误；没有自动化工具时，至少运行JavaScript语法检查。
3. 重新计算所有受影响的百分比、去重数量和平均值。
4. 确认“演示数据”、匿名化说明和“多校对比待接入”状态未被误删。
5. 如果视觉有变化，更新 `artifacts/` 中对应截图。

交付时简要说明修改文件、验证结果以及仍未实现的范围。

## 任务完成与发布

每次完成会修改仓库内容的任务后，必须按以下顺序收尾：

1. 更新 `readme.md`，确保使用方法、项目结构和当前边界仍然准确。
2. 更新 `handoff.md`，记录本次完成内容、验证结果、遗留范围和下一步。
3. 运行与改动相称的最低验证，检查 `git status`，只暂存本次任务相关文件。
4. 在本地 `main` 创建简洁提交，并直接推送到 `origin/main`，不创建功能分支或PR。
5. 推送前先获取远端最新状态；如出现冲突、认证失败或远端包含未知变更，停止并报告，不得使用强制推送。

如果任务没有产生任何仓库文件变化，不创建空提交，也不需要推送。
