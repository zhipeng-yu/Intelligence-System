# AGENTS.md

## 开始前

1. 完整阅读 `readme.md`、`handoff.md` 和相关代码。
2. 确认位于 `main`，先同步远端；禁止强制推送。
3. 当前代码是旧看板，目标范围以 `handoff.md` 为准。

## 实施约束

- 保持单个 `index.html`，使用原生HTML、CSS和JavaScript，不增加依赖或构建工具。
- 只做学校概况、资料清单、缺失资料；旧经营模块本轮移除。
- 资料只分五类：学校信息、教学进度、试卷资料、家长与情报、活动与产品。
- 每条资料只保留标题、分类、年级/学科、更新时间、负责人、确认状态、原文链接。
- 演示数据放在JavaScript数组中；不做录入、上传、Excel、持久化、后台、AI、多校或复杂对象关系。
- 使用匿名演示信息，不写入真实学生、家长、微信号或未授权内容。
- 只改任务需要的内容；优先删除旧代码，不为未来需求预留抽象。

## 验证

- 在电脑端Edge或Chrome检查页面。
- 验证搜索、分类筛选和原文链接。
- 检查无脚本错误，并更新视觉截图。
- 确认用户能直接看到缺失资料。

## 文档与发布

任务完成后精简更新 `readme.md`、`handoff.md`、`AGENTS.md`：只保留当前、可执行且代码中看不出的信息，先删过期重复内容。

验证后只暂存本次文件，在本地 `main` 创建简洁提交并直接推送 `origin/main`，不创建分支或PR。遇到冲突、认证失败或未知远端变更时停止，不得强推；无文件变化时不创建空提交。
