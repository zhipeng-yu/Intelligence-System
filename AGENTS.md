# AGENTS.md

## 开始前

完整阅读 `readme.md`、`handoff.md`、`school-profile-handoff.md` 和相关代码；确认位于 `main`，fetch 后只读核对 `origin/main` 未出现未知变化。只允许正常快进推送，禁止 pull 和强推。

涉及网络资料时使用 ponytail full，并完整阅读固定提交 `afa96802d3e61cdd5e7bd7b37ec59182bbe07d37` 对应的已安装 `xiaohongshu-skill` 指令与相关浏览器、登录、主页和详情代码。

## 当前状态

小规模多用户网络资料 MVP、访问预算优化、“小红书号”异步核验和浏览器自由选择均已上线；生产部署 `4310e4b9` 运行源码 `6e2ff6f`，远端 D1 已到 `0007_resolve_red_ids.sql`，既有 Secret 未修改。`ADMIN_KEY` 已于 2026-09-02 轮换并随当前部署生效。真实只读账号核验与有结果检索已验收；`Ledu-Network-Materials-Worker` 已恢复为每分钟触发、`MultipleInstances IgnoreNew`、状态 `Ready`，本地及 D1 停机标志均为关闭，恢复后的空闲轮询返回 0。

旧 `Ledu-Xiaohongshu-Course-Trial` 已禁用但未删除；旧 `seen.json`、运行状态和 `held_candidates` 已原样保留，禁用前后文件哈希一致。不得删除历史候选。

网络资料效率、访问预算、任务可观测性、小红书号核验和浏览器自由选择已在生产生效；实现与验证结果见 `new-window-prompt.md`。有结果验收账号为“杨老师的陪跑日记”、小红书号 `9522680303`；关键词“学习”、近 7 日的检索读取 20 条主页候选，得到 6 条窗口内图文，打开并检查 6 条详情，命中并保存 5 条，任务正常完成。

## 维护边界

- 保留单个原生 `index.html`、Pages Functions、D1、既有 R2、当前 Conda 和 Windows 任务计划；不增加框架、构建体系、第三方队列或新服务。
- 固定单校与两个登录后一级板块。学校资料共享原八张卡片；第九张 `other_products` 及历史数据保留但从画像、资料列表和撤销范围隐藏。
- 手机号登录不增加密码、短信、邀请码、自助注册或自助注销。完整手机号不得落库或记录；会话不得明文存储或写入 localStorage。
- 所有用户 API 从安全 Cookie 会话确定 `user_id`。管理员白名单和原敏感管理操作同时要求会话与 `ADMIN_KEY`；禁用用户删除会话但保留数据。
- 文件仍只支持 PDF、DOCX、XLSX，最大 50MB；Turnstile、网络散列限流、扩展名、MIME、文件头、私有 R2、随机对象键、附件下载、线性撤销和彻底删除规则不得弱化。
- `INGEST_KEY` 只保留既有 `POST /api/documents` 权限；工作器只接受独立 `NETWORK_WORKER_KEY`。
- 网络资料固定为每人 3 个账号、1～2 个 AND 关键词、近 1/3/7 日、每账号 20 条主页候选且只打开时间窗口内的图文详情、每任务 30 条结果、每人每天 3 次、全站每天 20 次、每人一个活动任务、最近 10 个结束任务。
- 添加账号只接受主页显示、大小写完全一致的“小红书号”，不得接受昵称、链接或内部 ID。核验每人每天 3 次、全站每天 20 次，删除不返还次数；只读一次用户搜索页前 20 个可见候选并最多打开一个精确匹配主页。核验与检索共用全局串行工作器，单用户两类任务不能同时活动。
- 网络资料不进入学校画像、R2/PDF 或 AI。不得保存完整文案、媒体、评论、用户资料或临时 token。
- 工作器保持全局串行，可使用指定或自动发现的可用本地浏览器。禁止 stealth、指纹伪装和验证码绕过；验证码、登录失效或安全验证必须 blocked、通知、停机并等待显式人工恢复。
- 不得恢复 1.0 的 36 字段、权重、候选、双确认，也不增加快照、重做、分支、知识图谱或向量库。
- 秘密不得进入 Git、聊天、日志、截图或普通文档；不读取或输出已有秘密、Cookie、管理链接、浏览器会话文件或原始凭证。

## 验证与发布

- 至少运行三组 Node 测试、两组 Python 测试、Pages Functions 构建和全新本地 `0001`～当前最新迁移。
- 页面修改和真实验收需要浏览器时优先使用 GPT 内置浏览器，检查主要流程、390px、主导航、键盘焦点和控制台，并更新 `artifacts/school-archive-desktop.png`。生产用户可能通过多种现代浏览器访问页面，产品实现不得依赖验收所用浏览器；只有本机工作器持久登录、任务计划或内置浏览器无法覆盖的系统级流程才改用其他可用浏览器。
- 只暂存本次文件；提交本地 `main` 后再次 fetch 并只读比较，再正常推送 `origin/main`。冲突、认证失败、非快进或未知远端变化立即停止。
- 推送代码不授权生产 D1、Secret、Pages 部署、计划任务恢复/重注册、真实小红书检索、真实 AI、旧任务删除或远端删除。

## 下一窗口的三个任务

下一窗口按 ponytail full 一并完成三项工作：让每个内部用户扫码绑定并使用自己的隔离小红书会话；定位并消除仍显示“24 位标准账号 ID”的旧页面入口或旧副本；完整审查小红书相关代码并删除确认无用的重复、兼容和遗留路径。当前生产仍是单个共享工作器 profile；仓库和标准生产域名实时 HTML 已使用“小红书号”表单，旧 24 位界面问题须先确认实际 URL、部署入口和浏览器缓存来源，不能重复改正确代码。三项任务完成并正式发布前不得把规划描述成已上线能力。具体范围与验收要求见 `new-window-prompt.md`。

## 每次任务收尾

按实际结果精简更新 `readme.md`、`handoff.md`、`school-profile-handoff.md` 和本文件，删除失效说明。除非用户明确要求不提交或不推送，验证通过后正常提交并快进推送。
