# 学校画像系统下一阶段交接

更新日期：2026-08-24

## 1. 交接目的

本文件用于把当前已经上线的“两人学校资料库”交接给下一阶段任务。现有生产部署、密钥、D1、R2 与运维命令仍以 `handoff.md` 为准；本文件只描述产品方向、已达成共识、待决策事项和建议执行顺序。

不得在本文、聊天或 Git 中记录 `ADMIN_KEY`、管理链接或其他秘密。

## 2. 当前仓库与生产状态

- 仓库：`https://github.com/zhipeng-yu/Intelligence-System.git`
- 生产临时地址：`https://ledu-school-archive.pages.dev`
- 维护分支：`main`
- 交接时本地 `HEAD`、本地 `origin/main` 与远端 `origin/main` 均为 `4bd6123de9e1c93111cafb8e518eb5bb8c59f102`
- 创建本交接文件前工作区干净，无既有未提交修改；本文件和 `new-window-prompt.md` 是本次新增内容
- 当前服务端测试：`node --test tests/api.test.mjs`，5 项通过

开始新任务前必须重新阅读 `AGENTS.md`、`readme.md`、`handoff.md`、本文和相关代码，并重新核对分支、工作区与远端；遇到未知远端变化立即停止。

## 3. 现有系统能力

当前版本是单学校、两名同权使用者的私有资料库：

- 原生单页 `index.html`，无前端框架、依赖或构建体系
- Cloudflare Pages Functions + D1 + 私有 R2
- 五类资料：学校信息、教学进度、试卷资料、家长与情报、活动与产品
- 支持 PDF、DOCX、XLSX，单文件最大 50MB
- 服务端校验扩展名、MIME、文件头和大小
- 支持上传、列表、搜索、筛选、下载、确认状态和软删除
- 全部资料 API 由共享 `ADMIN_KEY` 保护

当前数据模型只有 `documents`。学校名称、当前缺失项等仍有页面静态内容，因此现阶段本质上仍是“文件资料库”，还不是学校画像系统。

## 4. 用户已明确的下一阶段目标

用户已经明确：

> 根据零散上传的信息，逐步生成一所学校的完整画像；即使画像尚未完成，也要展示百分比进度。

这意味着下一阶段产品中心从“文件管理”转向“学校画像生成”。

最终应该让使用者打开一所学校后直接知道：

- 当前画像完成了多少
- 已经确认掌握了什么
- 哪些信息只是候选或待验证
- 哪些信息发生冲突或已经过期
- 画像还缺哪些关键字段
- 补充什么信息最能提高画像可用性
- 每个结论来自哪一份资料或哪一个信息来源

## 5. 已达成的核心产品原则

### 5.1 资料是证据，画像字段是结论

文件数量不能直接提高画像完成度。一份文件只有补充或验证了画像字段，才会影响进度。

同一份文件可以支持多个画像字段；同一个画像字段也可以由多份文件共同验证。

### 5.2 先做画像引擎，再做自动提取

下一阶段有两个不同能力：

1. 画像引擎：定义字段、权重、状态、证据关系并计算完成度。
2. 提取引擎：从 PDF、DOCX、XLSX 或手工记录中提取候选信息。

必须先稳定画像引擎。否则自动提取出来的信息没有统一位置、状态和评分规则可写入。

### 5.3 AI只能产生候选信息

如果后续引入 AI，AI提取内容默认只能进入“待确认”，不能直接成为已验证事实。重要结论必须保留来源和证据定位。

### 5.4 进度不能按文件数计算

完成度应按预先定义的画像字段及权重计算，避免上传大量重复资料制造虚假进度。

### 5.5 一个百分比不够

建议最终同时展示：

- 完成度：画像字段覆盖了多少
- 可信度：已覆盖字段中有多少经过确认或证据验证
- 新鲜度：已确认字段中有多少仍在有效期内

第一版至少必须实现完成度；可信度和新鲜度可以在字段状态与时效规则稳定后加入。

## 6. 建议的学校画像标准 V1

以下是讨论中形成的建议方案，尚未由用户最终确认；新任务应先将其整理为正式字段清单。

| 画像维度 | 建议权重 | 主要内容 |
| --- | ---: | --- |
| 学校基本盘 | 15% | 学校类型、校区、招生范围、规模、校历、作息 |
| 年级与班型 | 15% | 年级人数、班级数量、班型、分班机制、班型差异 |
| 教学地图 | 25% | 教材、进度、教辅、班级差异、教学难点、授课特点 |
| 考试与学情 | 15% | 考试节奏、范围、试卷、难度、高频题型、常见失分点 |
| 用户与情报 | 10% | 覆盖情况、主要反馈、待验证情报、来源数量 |
| 内容与产品 | 10% | 讲座、诊断、专项资料、课程、已有与缺失产品 |
| 活动与结果 | 10% | 活动、参与、完成、复参与、反馈、有效产品 |

第一版不要一次铺开所有年级、学科和学校。建议先选“一所真实学校 + 一个学期”，教学相关字段再限定到“一个年级 + 一个学科”作为试点。

## 7. 建议的字段状态与进度规则

建议字段状态：

- 缺失：没有信息，不计进度
- 待确认：已从资料或人工录入中获得候选信息，计部分进度
- 已确认：已经人工确认或有可靠证据，计全部进度
- 有冲突：存在互相矛盾的信息，不应按已完成计算
- 已过期：超过字段有效期，需要重新收集

建议第一版使用简单系数：

- 缺失：0
- 待确认：0.5
- 已确认：1
- 有冲突：0.5 或单独展示，不得算满
- 已过期：0 或按待确认处理

完成度公式：

```text
完成度 = Σ（字段权重 × 状态系数）÷ Σ（全部字段权重）
```

具体系数和字段有效期仍需在 `profile-schema-v1.md` 中定稿。页面必须能解释分数来源，不能只显示一个无法追溯的百分比。

## 8. 推荐的第一阶段范围

第一阶段目标是形成最小完整闭环：

```text
零散资料 → 候选信息 → 人工确认 → 更新画像字段
        → 重新计算完成度 → 展示已知与缺失 → 回到证据
```

第一阶段应包含：

- 一份固定的学校画像字段清单与权重
- 字段的缺失、待确认、已确认、冲突、过期状态
- 画像总完成度和分维度完成度
- 画像字段与现有资料的证据关联
- 已知、待确认、冲突、过期和缺失列表
- 从画像结论返回原始资料的能力
- 上传后以人工确认方式补充画像字段的最小路径

第一阶段暂不包含：

- 多学校经营驾驶舱
- 知识图谱数据库
- 自动课程生成
- 自由问答式 AI 助手
- 家长或学生 CRM
- 复杂账号、角色和部门后台
- 自动经营预测
- 将未经确认的 AI 输出直接写成事实

## 9. 自动提取的关键待决策事项

当前项目边界明确写着“不实现 AI、全文解析”。而“根据零散上传的信息自动生成画像”最终可能需要文档解析或外部 AI 服务，因此这是下一阶段必须显式决策的范围变化，不能默认突破。

建议顺序：

1. 先实现不依赖外部 AI 的画像字段、证据关系和进度计算。
2. 使用人工确认的候选信息验证字段标准是否合理。
3. 字段标准稳定后，再选择自动提取方案。
4. 若要把私有资料发送到外部 AI，必须先确认服务、数据使用方式、权限、费用、日志、删除机制和敏感信息边界。

没有明确授权前，不得把生产资料上传到任何第三方 AI 或解析服务。

## 10. 安全与隐私边界

- 现有共享密钥只适合两人试点，不是身份审计。
- 第一阶段优先保存学校层面和汇总信息，避免增加学生、家长的敏感明细。
- 不得弱化现有文件类型、文件头、MIME、50MB、鉴权、私有 R2、随机对象键、附件下载和软删除边界。
- 新增 API 必须继续使用统一鉴权。
- 所有用户输入继续通过安全 DOM API 渲染。
- 如果未来扩大到多名老师或存储个人敏感信息，应先升级真实账号、权限和操作审计。

## 11. 新窗口建议执行顺序

1. 完整阅读项目说明、交接文档和代码，核对仓库状态。
2. 创建并定稿 `profile-schema-v1.md`：列出画像维度、具体字段、权重、状态、有效期、可接受证据和示例。
3. 检查所有字段的总权重、评分可解释性和缺失提示逻辑。
4. 给出最小数据模型、API、页面变化和迁移方案；保留现有资料能力。
5. 先实现画像引擎和人工确认闭环，不默认接入外部 AI。
6. 增加服务端测试，并完成与页面改动对应的浏览器验收。
7. 更新 `readme.md`、`handoff.md`、截图和本交接文件中的实际状态。
8. 只有在用户明确要求发布时才部署；提交和推送必须遵守 `AGENTS.md`。

## 12. 第一阶段验收标准

- 零资料时画像显示 0%，并列出全部关键缺失项。
- 待确认信息只能获得部分进度，不能伪装成已确认事实。
- 确认字段后，总完成度和对应维度完成度按照固定权重变化。
- 删除、失效或标记冲突后，进度能够正确回退。
- 每个已确认结论能够看到来源，并在有关联文件时返回原始资料。
- 多份重复资料不会重复提高同一字段的完成度。
- 页面能够解释“为什么是这个百分比”以及“下一步补什么最有效”。
- 现有上传、搜索、筛选、下载、确认、软删除和安全校验继续可用。

## 13. 北极星判断

这一阶段是否成功，不看上传了多少文件，而看：

> 使用者能否在不翻阅全部资料的情况下，迅速理解这所学校当前已知什么、缺什么、这些结论是否可信，以及画像距离可用还差多少。

## 14. 2026-08-24 实施结果

本阶段已在本地形成可验证的最小画像闭环，尚未部署生产：

- `profile-schema-v1.md` 定稿为 7 个维度、36 个字段、总权重 100%，限定单学校、当前学期和一个试点年级/学科。
- 待确认系数定为 0.25；已确认 1；缺失、冲突和过期均为 0。字段按固定权重封顶，不按文件或候选数量累加。
- 新增 `profile_values` 一个 D1 表；候选直接关联现有资料和来源定位，没有引入证据图谱、中间服务或新依赖。
- 新增 `/api/profile` 查询与候选创建、更新、软删除接口；全部继续使用统一管理密钥鉴权。
- 新候选只能是待确认；来源资料未确认时服务端拒绝确认候选。重复相同结论不加分，不同的有效已确认值自动形成冲突。
- 来源资料退回待确认、过期、候选软删除或来源资料软删除后，完成度按规则回退。
- 页面展示总完成度、分维度完成度、状态数量、固定权重贡献、下一步最高收益字段、候选值、有效期、来源定位和来源下载。
- 未接入外部 AI、文档解析服务或新的外部数据流，也未发送任何生产资料。

本地验证结果：现有资料 API 5 项测试和画像 5 项测试通过；D1 迁移和 Pages Functions 构建通过；真实本地 API 闭环验证了 `0% → 1.5% → 6% → 0%` 及来源删除回退。电脑端 Microsoft Edge 验证了上传、候选、确认拦截、完成度增减、重复拦截、搜索筛选、来源追溯、390px 响应式布局和键盘焦点，控制台无警告或错误；桌面截图已更新。生产远端迁移和部署未执行。
