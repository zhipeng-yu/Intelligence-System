# 学校画像系统 2.0 技术交接

更新日期：2026-08-26

## 结论

仓库源码已按 ponytail full 完成碎片累积与线性撤销的最小实现：复用 `profile_sections`、`profile_history` 和 `documents`，没有新增完整快照表、独立来源表、框架、依赖、队列、重做、分支或任意版本恢复。

生产仍只完成 `0002` 迁移并运行旧版 2.0，当前生产部署源码提交为 `f0234f3`；`0003`、新源码和新页面均未部署。

## 页面与权限

老师首页只有：

1. 学校信息概览
2. 文件 + 一句可选备注的上传
3. 最近更新

八张卡片固定为：学校概况、校历与作息、年级与班级概况、教材与当前教学进度、考试安排与范围、教学重点难点与常见失分、近期活动与通知、可用教学资源。完成度是非空卡片数除以 8。

公开访问可查看卡片、当前有效的卡片级参考资料和上传。管理链接显示原文件下载、失败重试、全局“撤销最近一次画像更新”和符合撤销顺序的彻底删除；人工编辑、解锁、锁定提示和画像 PATCH API 已删除。

## AI 闭环

```text
公开上传
-> Turnstile + 每网络散列每小时 5 份
-> 私有 R2 与 D1
-> 浏览器触发一次 /analyze
-> env.AI.toMarkdown
-> 原生 fetch 调用方舟 Responses API
-> function_call 条目数组白名单校验
-> 只写入实际变化的受影响卡片
```

- 模型固定为 `doubao-seed-2-0-lite-260215`，关闭深度思考，不安装 SDK。
- 每次把当前八卡片与新资料一起交给 AI；新资料优先，保留不冲突旧项、替换冲突项、删除过时项并去重。
- 每份文件只允许一次公开自动整理；失败后只有管理员可重试，已撤销资料不能重新整理。
- AI 每卡返回 1–12 个非空短条目，总长不超过 4000 字；未返回卡片不清空，完全相同结果不写库。
- 实际变化前始终把旧内容、旧来源和旧更新时间写入隐藏历史，包括空内容。
- 转换或 AI 失败时文件保留，并记录简短失败状态。
- 资料正文明确按不可信数据处理；仅提取文档明示事实。

## 数据模型

`documents` 保留文件、可选备注、可选 AI 分类/范围、AI 状态、错误、分析时间、一次性标记、网络散列、旧软删除列和 `undone_at`。新代码不再写软删除列。

`profile_sections` 保留八个固定 key、当前内容、来源文档和更新时间；旧 `locked` 列保留但运行时不再读写。`profile_history` 复用旧值，并由 `change_document_id` 将一份资料影响的多卡组成一个撤销步骤，`previous_updated_at` 保存更新时间恢复点。

旧的 `0001` 是生产基线；`0002` 已在 2026-08-25 应用于生产，因此不得重写。`0003_add_linear_undo.sql` 只增加三个确认字段，并从现有来源、更新时间和历史安全补出撤销起点。

来源列表从当前卡片来源和仍有效、未撤销的历史变更推导，不重复存储。撤销只选择最新有效资料，一次恢复该资料的全部历史行并设置 `undone_at`。彻底删除先验证资料不在有效栈中，再删除 R2、清除历史文件身份引用并硬删除 D1 文档；历史内容永久匿名保留。

## 已验证

- API 测试：6 项通过。
- 画像/AI/撤销测试：7 项通过。
- 全新本地迁移：`0001`、`0002`、`0003` 均成功。
- 已有状态回填：本地 `A → B` 夹具正确生成两个线性撤销起点和前一更新时间。
- Pages Functions 构建成功。
- 模拟 D1/R2/方舟闭环覆盖当前画像输入、新资料合并、同值跳过、多卡整体撤销、A/B/C 顺序、失败重试、撤销后禁止重试、硬删除、匿名历史和鉴权。
- 新页面已用电脑端 Edge 验收桌面、390px 单列布局、键盘焦点、来源展开、失败重试入口、线性撤销和永久删除确认；控制台无错误，`artifacts/school-archive-desktop.png` 已更新。
- 生产 D1 `0002` 已应用，Pages 部署 `15ff5cd8-1c2b-4c5f-a33b-8ea21894c769` 已成为最新生产版本。
- 旧版生产验收时：首页与部署地址返回 200，公开画像 API 为 1/8 且包含八张卡片，Turnstile 配置已生效。
- 旧版生产验收时：公开资料列表为 1，资料状态为 `completed`，无密钥删除请求返回 401，远端当时无待执行迁移。
- 经用户明确授权，旧版生产曾重试一份此前失败的资料，真实 Workers AI 转换、方舟 Responses API、白名单解析和 D1 写入均成功。

自动化测试仅使用模拟方舟响应，不读取真实方舟密钥；新源码的 Edge 验收使用纯本地模拟 API，没有访问生产数据或触发真实 AI。

## 发布边界

生产已将 `ARK_API_KEY`、`TURNSTILE_SECRET`、`ADMIN_KEY` 和 `TURNSTILE_SITE_KEY` 配置为 Cloudflare Secret；`AI` 是 Workers AI binding。具体资源和后续发布顺序见 `handoff.md`。

代码提交和快进推送不授权远端 D1 `0003`、Pages 部署、生产硬删除或真实生产 AI 调用；这些动作需逐次获得明确授权。禁止强推，远端出现未知变化立即停止。
