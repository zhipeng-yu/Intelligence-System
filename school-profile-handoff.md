# 学校资料与网络资料技术交接

更新日期：2026-09-09。生产已发布 `cfa1f6f1` / `aa4f3a6` / `0008`；工作器已切换源码，计划任务暂停，等待两用户真实扫码验收。

## 保持不变

单页、Pages Functions、D1、既有私有R2、Conda和Windows任务计划；共享八张学校画像卡片，other_products及历史数据保留隐藏。手机号HMAC、会话SHA-256、安全Cookie、白名单禁用、敏感管理双认证、文件类型/大小/签名/限流/私有下载/线性撤销边界不变。学校AI链路、INGEST_KEY权限不变。网络资料不进入学校画像、R2/PDF或AI。

## 绑定和隔离

0008新增network_bindings，每用户一行，随机UUID profile_id与request_id；保存状态、挑战公钥/密文、发起会话哈希和绑定租约。原账号、任务、结果没有迁移或删除。禁用用户触发失效和挑战清除。

用户接口 `GET/POST /api/network/binding` 从Cookie确定user_id。POST bind/unbind至少间隔10秒；运行中的核验或检索阻止切换，排队中的资料任务等待绑定完成。账号申请触发器与检索条件插入要求ready绑定。三类认领均排除禁用用户。

刷新变更request_id但不会提前释放旧lease；工作器心跳发现旧请求、会话失效或禁用后停止，关闭浏览器，清理自己的profile，旧完成回传不能覆盖新请求。绑定等待上限120秒，租约180秒；过期释放队列。资料工作保留原50分钟租约、40分钟访问截止、配额及180次全站日预算。

工作器仅使用 `network-worker/profiles/<profile_id>`；验证UUID和解析后的目录范围，拒绝目录跳转。解绑/重绑清理这个目录，保留其他用户和旧共享profile。全局D1互斥、本机文件锁和IgnoreNew共同限制并发。旧工作器没有user_sessions协议，无法认领新工作。

二维码只截取元素到内存，不调用技能写固定二维码文件或整页截图的方法。服务端使用WebCrypto AES-GCM加密图片，再用发起页面RSA-OAEP公钥封装临时密钥；私钥不可导出且仅存在页面内存。D1仅存挑战密文/公钥；取图需发起会话有效，同一内部用户的其他会话也不返回挑战。刷新、离开、退出或过期清除页面密钥与Blob；终态、解绑、禁用及过期清理删除挑战。后台停机期间残余密文在下次认领/查询清理，备份中没有解密私钥。

浏览器前端使用标准WebCrypto、Blob URL、fetch和原生表单，没有依赖工作器浏览器Cookie、特定浏览器扩展或用户代理判断。真实跨浏览器/扫码验收已获授权，等待用户扫码参与。

验证码、安全验证、登录失效仍全局blocked、通知、停机；人工恢复必须指定对应profile_id。单账号普通页面错误只保存固定消息，避免浏览器异常中的临时URL进入结果。

## 清理与保留

xhs_course_trial.py保留现行工作器实际引用的浏览器适配、固定技能哈希校验、DPAPI、通知、时间与详情解析；直接运行旧模块只报告退役。删除旧七日基线、采集、PDF、上传/AI、恢复、注册入口及注册脚本源码。实际旧计划任务和全部历史候选未动。

保留DPAPI既有entropy以兼容已有NETWORK_WORKER_KEY密文；保留0001～0007迁移、旧账号无red_id的展示、旧摘要内部ID遮蔽、未记录漏斗的旧任务状态，以及既有文档INGEST_KEY与other_products权限隔离。它们仍有数据或调用依赖。

## 验证

Node33项、Python18项通过，Pages Functions构建和全新本地0001～0008迁移成功。两用户隔离、所有者加密挑战、扫码过期、刷新、解绑、重绑、禁用、队列和blocked均以合成数据验证。内置浏览器完成桌面/390px/键盘/主导航/模拟绑定；控制台无错误。生产发布顺序与授权边界见handoff.md。

本轮代码/测试/迁移总行数6700→6686，净减少14行（不含文档与图片，包含新增绑定及本地验收代码）。
