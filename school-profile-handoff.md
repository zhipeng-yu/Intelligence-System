# 学校画像系统 2.0 技术交接

更新日期：2026-08-31

## 结论

仓库源码已按 ponytail full 完成碎片累积与线性撤销的最小实现：复用 `profile_sections`、`profile_history` 和 `documents`，没有新增完整快照表、独立来源表、框架、依赖、队列、重做、分支或任意版本恢复。

生产已应用到 `0004_add_other_products_section.sql` 并部署源码提交 `538f8c6`，当前页面与 API 均运行碎片累积、线性撤销、符合顺序的彻底删除、九张卡片和最小权限机器上传。

## 页面与权限

老师首页只有：

1. 学校信息概览
2. 文件 + 一句可选备注的上传
3. 最近更新

九张卡片固定为：学校概况、校历与作息、年级与班级概况、教材与当前教学进度、考试安排与范围、教学重点难点与常见失分、近期活动与通知、可用教学资源、其他产品资料。完成度是非空卡片数除以 9。

公开访问可查看卡片、当前有效的卡片级参考资料和上传。管理链接显示原文件下载、失败重试、全局“撤销”按钮和符合撤销顺序的彻底删除；人工编辑、解锁、锁定提示和画像 PATCH API 已删除。

小红书试运行的机器上传只在现有 `POST /api/documents` 接受独立 `X-Ingest-Key`。正确密钥仅绕过 Turnstile，网络散列限流、PDF/DOCX/XLSX、50MB、扩展名、MIME 和文件头校验均保持；该密钥不能访问任何管理接口。机器资料固定标记为“其他产品资料”，分析时只能更新第九卡；普通上传也不能写入第九卡。

## AI 闭环

```text
公开上传
-> Turnstile + 每网络散列每小时 5 份
-> 私有 R2 与 D1
-> 浏览器触发一次 /analyze
-> env.AI.toMarkdown
-> 原生 fetch 调用方舟 Responses API
-> function_call 条目数组白名单校验
-> 只写入实际变化的受影响卡片
```

- 模型固定为 `doubao-seed-2-0-lite-260215`，关闭深度思考，不安装 SDK。
- 普通资料把当前八张学校画像与新资料交给 AI；小红书机器资料只把第九卡和当日公开笔记交给 AI，并保留最新 12 条。两条路径都由工具 schema 和返回解析强制白名单隔离。
- 每份文件只允许一次公开自动整理；失败后只有管理员可重试，已撤销资料不能重新整理。
- AI 每卡返回 1–12 个非空短条目，总长不超过 4000 字；未返回卡片不清空，完全相同结果不写库。
- 实际变化前始终把旧内容、旧来源和旧更新时间写入隐藏历史，包括空内容。
- 转换或 AI 失败时文件保留，并记录简短失败状态。
- 资料正文明确按不可信数据处理；仅提取文档明示事实。

## 数据模型

`documents` 保留文件、可选备注、可选 AI 分类/范围、AI 状态、错误、分析时间、一次性标记、网络散列、旧软删除列和 `undone_at`。新代码不再写软删除列。

`profile_sections` 保留九个固定 key、当前内容、来源文档和更新时间；旧 `locked` 列保留但运行时不再读写。`profile_history` 复用旧值，并由 `change_document_id` 将一份资料影响的多卡组成一个撤销步骤，`previous_updated_at` 保存更新时间恢复点。

旧的 `0001` 是生产基线；`0002` 已在 2026-08-25、`0003_add_linear_undo.sql` 已在 2026-08-26 应用于生产，因此均不得重写。`0004_add_other_products_section.sql` 只无损重建 `profile_sections` 的 key 约束、保留原八行并增加 `other_products`，不新增表。

来源列表从当前卡片来源和仍有效、未撤销的历史变更推导，不重复存储。撤销只选择最新有效资料，一次恢复该资料的全部历史行并设置 `undone_at`。彻底删除先验证资料不在有效栈中，再删除 R2、清除历史文件身份引用并硬删除 D1 文档；历史内容永久匿名保留。

## 已验证

- API 测试：8 项通过，包含正确/错误 `INGEST_KEY`、机器资料标记和公开 Turnstile 不回退。
- 画像/AI/撤销测试：9 项通过，包含第九卡隔离、拒绝污染原八卡和第九卡撤销。
- 小红书试运行 Python 测试：9 项通过；新增只取已见锚点前的连续前缀并记录全部新增笔记，历史候选保留但不进入自动队列。
- 全新临时本地迁移：`0001`、`0002`、`0003`、`0004` 均成功，结果恰好 9 行；带模拟旧卡执行 `0004` 后内容、来源、更新时间和锁定值均保留。
- 已有状态回填：本地 `A → B` 夹具正确生成两个线性撤销起点和前一更新时间。
- Pages Functions 构建成功。
- 模拟 D1/R2/方舟闭环覆盖当前画像输入、新资料合并、同值跳过、多卡整体撤销、A/B/C 顺序、失败重试、撤销后禁止重试、硬删除、匿名历史和鉴权。
- 新页面已用系统 Edge 验收 9 卡桌面、390px 单列、键盘焦点和控制台错误；控制台无错误，`artifacts/school-archive-desktop.png` 已更新。
- 本地 Pages API 闭环返回 9 卡；错误机器密钥为 400，正确密钥为 201 且文档固定标记为“其他产品资料”。
- 生产 D1 已应用到 `0004` 且无待执行迁移；Pages 已从干净的 `main` 部署源码提交 `538f8c6`，部署短标识为 `35577459`。
- 生产公开画像返回 200 和九张卡片；系统 Edge 在 390px 下确认九卡可见、无横向溢出且控制台无错误。
- 历史演示笔记“学而思大阅读 阅写双提升”已完成机器上传和 1 次生产 AI，文档状态为 `completed`、作用域仅“其他产品资料”；第九卡有 1 个来源，原八卡前后校验一致。
- 七日 Windows 任务为 Ready，上次 2026-08-31 09:00 成功，下一次 2026-09-01 09:00，结束日仍为 2026-09-04；当前配额已用 1/7、无待上传和活动批次，旧历史候选仍完整保留。
- 经用户明确授权，无敏感信息的合成 PDF 完成真实 Workers AI 转换、方舟 Responses API、白名单解析和 D1 写入，只生成 1 个画像撤销步骤。
- 生产线性撤销恢复到原画像 1/8，合成资料状态变为“更新已撤销”；随后应用内硬删除成功，R2 对象和 D1 文件身份消失，原有资料保持 1 份，三类画像引用均无悬空记录。

自动化测试仍只使用模拟方舟响应且不读取真实方舟密钥；生产真实 AI 验收仅使用任务内生成、无敏感信息的合成资料，并在验证撤销后彻底删除。

## 发布边界

生产已将 `ARK_API_KEY`、`TURNSTILE_SECRET`、`ADMIN_KEY`、`INGEST_KEY` 和 `TURNSTILE_SITE_KEY` 配置为 Cloudflare Secret；`AI` 是 Workers AI binding。试运行实时状态见 `handoff.md`。

本轮授权的 `0004` 远端迁移、Pages 部署和一次历史演示生产 AI 均已完成；试运行七日内最多 7 次真实生产 AI 由本地配额控制，当前已用 1 次。生产硬删除、超额 AI 和其他生产动作仍需明确授权。禁止强推，远端出现未知变化立即停止。
