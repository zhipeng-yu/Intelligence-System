# 学校画像系统 2.0 技术交接

更新日期：2026-08-26

## 结论

2.0 已按确认方案完成最小重构：1.0 的 36 字段、七维权重、候选、状态系数和资料/候选双确认均已删除。实现不是在 1.0 上叠加功能，而是固定八张卡片、一次上传、一次自动整理的简单闭环。

生产已完成 `0002` 迁移并部署 2.0，当前生产部署源码提交为 `f0234f3`。

## 页面与权限

老师首页只有：

1. 学校信息概览
2. 文件 + 一句可选备注的上传
3. 最近更新

八张卡片固定为：学校概况、校历与作息、年级与班级概况、教材与当前教学进度、考试安排与范围、教学重点难点与常见失分、近期活动与通知、可用教学资源。完成度是非空卡片数除以 8。

公开访问可查看卡片、来源文件名和上传。管理链接才显示人工编辑/解锁、原文件下载、软删除和失败重试。人工编辑保存后立即锁定，后续 AI 跳过该卡片；解锁后才允许再次自动更新。

## AI 闭环

```text
公开上传
-> Turnstile + 每网络散列每小时 5 份
-> 私有 R2 与 D1
-> 浏览器触发一次 /analyze
-> env.AI.toMarkdown
-> 原生 fetch 调用方舟 Responses API
-> function_call 参数白名单校验
-> 只写入本次返回且未锁定的卡片
```

- 模型固定为 `doubao-seed-2-0-lite-260215`，关闭深度思考，不安装 SDK。
- 每份文件只允许一次公开自动整理；失败后只有管理员可重试。
- AI 未返回的卡片不清空；被覆盖的旧值写入隐藏历史。
- 转换或 AI 失败时文件保留，并记录简短失败状态。
- 资料正文明确按不可信数据处理；仅提取文档明示事实。

## 数据模型

`documents` 只保留文件、可选备注、可选 AI 分类/范围、AI 状态、错误、分析时间、一次性标记、网络散列和软删除字段。

`profile_sections` 只保留八个固定 key、当前内容、来源文档、更新时间和人工锁定；`profile_history` 保留被替换的旧值。

旧的 `0001` 是生产基线；`0002` 已在 2026-08-25 应用于生产，因此后续不得再重写已上线迁移。

## 已验证

- API 测试：5 项通过。
- 画像/AI 测试：5 项通过。
- 全新本地迁移：`0001`、`0002` 均成功。
- Pages Functions 构建成功。
- 真实本地 D1/R2 闭环成功：公开列表、管理员上传、无密钥下载拒绝、管理下载、卡片编辑/解锁、软删除。
- Edge 桌面 1440px 已确认八卡片双列、三块页面结构、无横向溢出；390px 为单列卡片和单列上传表单。
- Edge 键盘 Tab 焦点为 3px 可见轮廓，Turnstile 测试组件正常，桌面与移动端控制台均无警告或错误。
- 桌面验收截图已更新：`artifacts/school-archive-desktop.png`。
- 生产 D1 `0002` 已应用，Pages 部署 `15ff5cd8-1c2b-4c5f-a33b-8ea21894c769` 已成为最新生产版本。
- 生产首页与本次部署地址返回 200；公开画像 API 为 1/8 且包含八张卡片，Turnstile 配置已生效。
- 生产公开资料列表为 1，资料状态为 `completed`；无密钥删除请求返回 401，远端无待执行迁移。
- 经用户明确授权，管理员重试一份此前失败的生产资料，真实 Workers AI 转换、方舟 Responses API、白名单解析和 D1 写入均成功。

自动化测试仍使用模拟方舟响应，不读取真实方舟密钥。生产页面已在 Edge 打开并确认管理模式和失败重试入口；长耗时整理期间浏览器控制连接超时，最终结果由生产 API 复核为 `completed` 和 1/8。本地 Edge 桌面、390px、焦点和控制台验收结果仍有效。

## 发布边界

生产已将 `ARK_API_KEY`、`TURNSTILE_SECRET`、`ADMIN_KEY` 和 `TURNSTILE_SITE_KEY` 配置为 Cloudflare Secret；`AI` 是 Workers AI binding。具体资源和后续发布顺序见 `handoff.md`。

后续远端 D1 迁移、Pages 部署或真实生产 AI 调用仍需逐次获得明确授权。禁止强推，远端出现未知变化立即停止。

## 下一版已确认方案（尚未实现）

碎片资料不再整卡盲目覆盖。每次整理都把当前八卡片与新资料交给 AI；新资料按上传顺序优先，AI 为受影响卡片返回完整新版短条目，保留不冲突旧项、替换冲突项、删除过时项并去重。每卡最多 12 条、总长度不超过 4000 字，完全相同的结果不写库。

管理员不再人工编辑或锁定卡片。一次成功资料整理是撤销栈的一步，即使同时更新多卡也整体撤销；只能像 Word 一样从最新一步向前撤销，无重做、分支、重新整理或任意历史恢复。卡片来源从当前值与仍有效的历史推导，不增加独立来源表。

实现复用现有 `profile_history`，计划由 `0003` 只增加 `documents.undone_at`、`profile_history.change_document_id` 和 `profile_history.previous_updated_at`，并用现有数据补出当前生产状态的撤销起点。不新建完整状态快照表。失败、未生效或已撤销资料才允许彻底删除 R2 和 D1 文件身份；匿名画像历史永久保留。完整工作提示见 `new-window-prompt.md`。
