# 学校画像系统 2.0 交接

更新日期：2026-08-24

## 1. 交接结论

用户已否决学校画像 1.0 的 36 字段、权重、候选信息和双确认流程，原因是普通教培老师难以理解、操作成本过高。下一阶段不是在 1.0 上继续叠加功能，而是删除复杂设计，改成“一次上传，AI 自动整理，老师直接看学校信息”的 2.0。

本文件记录已经确认的最终方案。新任务不得重新引入候选确认、复杂状态、知识图谱、向量库、账号体系或多学校架构。

不得在本文、聊天或 Git 中记录 `ADMIN_KEY`、`ARK_API_KEY`、Turnstile Secret、管理链接或其他秘密。

## 2. 当前实际状态

- 仓库：`https://github.com/zhipeng-yu/Intelligence-System.git`
- 维护分支：`main`
- 交接前本地与 `origin/main` 均为 `990ce04516f7dde6af474041ace169bf0c4836f9`
- 生产地址：`https://ledu-school-archive.pages.dev`
- 生产仍运行旧的两人资料库版本。
- 仓库中的学校画像 1.0 已实现并通过本地测试与 Edge 验收，但尚未部署生产。
- 远端 D1 尚未应用 `0002_create_profile_values.sql`。
- 画像 1.0 当前包含 36 字段、固定权重、候选、待确认/已确认/冲突/过期状态和双确认；这些是下一任务要删除的对象，不是继续保留的产品边界。

开始新任务前必须完整阅读 `AGENTS.md`、`readme.md`、`handoff.md`、本文、`profile-schema-v1.md` 和现有页面、Functions、迁移、测试；确认位于 `main`，工作区正常，并只读核对远端。遇到冲突、认证失败或未知远端变化立即停止。

## 3. 使用者与范围

- 只服务当前一所学校，不做多学校或多租户。
- 系统使用者是公司内部从事教培的老师。
- 学生和家长是公司服务对象，不是系统账号。
- 公司老师无需注册或登录即可查看画像和上传资料。
- 产品定位是公司内部工具，不作为面向社会公众的生成式 AI 产品推广。
- 现有管理链接继续用于管理操作，不增加账号、部门、角色或权限后台。

## 4. 老师端页面

老师端只保留三个核心区域：

1. 学校信息概览
2. 极简资料上传
3. 最近更新

老师不应看到：

- 36 个字段长列表
- 七个维度和字段权重
- 候选结论登记
- 待确认、已确认、有冲突、已过期状态
- 资料确认和候选确认
- 复杂缺失清单
- 下载、删除、重新分析等管理操作

## 5. 八张画像卡片

前台固定展示八张卡片：

1. 学校概况
2. 校历与作息
3. 年级与班级概况
4. 教材与当前教学进度
5. 考试安排与范围
6. 教学重点、难点与常见失分
7. 近期活动与通知
8. 可用教学资源

每张卡片只展示当前内容、更新时间和“查看来源”。来源资料、旧版本、AI 处理记录和人工锁定状态放在详情或管理态中，不占老师首页。

## 6. 简单完成度

删除全部权重和状态系数。完成度只按八张卡片是否有内容计算：

```text
完成度 = 有内容的卡片数 ÷ 8
```

页面文案示例：

> 学校信息已补全 5/8（63%）

文件数量本身不计分。

## 7. 极简上传

上传表单只保留：

- 拖入或选择文件
- 一句可选备注
- 上传按钮

删除手动标题、分类、年级/学科、上传者和确认状态。文件名作为默认标题，分类、年级、学科和画像信息由 AI 识别。

上传区域必须明确提示：

> 请勿上传学生名单、手机号、个人成绩明细、身份证信息或公司保密资料。

继续支持 PDF、DOCX、XLSX，单文件最大 50MB；扩展名、MIME、文件头和大小仍由服务端校验。

## 8. 已确认的 AI 数据流（方案 B）

用户已明确授权使用火山方舟外部 AI，并选择保留三种文件格式的方案 B：

```text
PDF / DOCX / XLSX
→ 原文件保存到私有 R2
→ Cloudflare Workers AI toMarkdown 转成文字
→ 只把提取后的文字和可选备注发送到火山方舟
→ Doubao-Seed-2.0-lite 返回需要更新的画像卡片
→ D1 保存结果，页面自动刷新
```

不使用火山方舟知识库、向量库、Bot、文件长期索引或第三方解析服务。火山方舟 File API 官方只明确直接预处理视频、图片和 PDF，因此保留 DOCX/XLSX 时使用一次 Cloudflare 文档转换是当前最小且有官方支持的路径。

火山方舟配置固定为：

- Base URL：`https://ark.cn-beijing.volces.com/api/v3`
- Responses API：`/responses`
- 模型：`doubao-seed-2-0-lite-260215`
- 使用原生 `fetch`，不安装 SDK
- 使用一次函数调用返回最多八张卡片
- 关闭或最小化深度思考，优先速度与费用
- API 密钥只使用 Cloudflare Secret `ARK_API_KEY`

官方参考：

- 模型与 Responses API：<https://www.volcengine.com/docs/82379/1795150>
- 价格：<https://www.volcengine.com/product/doubao/>
- 豆包模型服务协议：<https://www.volcengine.com/docs/82379/1142195>
- Cloudflare 文档转换格式：<https://developers.cloudflare.com/workers-ai/features/markdown-conversion/supported-formats/>

豆包模型服务协议当前说明，API 输入输出默认不用于训练基础模型，除非账号主动同意数据授权或参加相关奖励计划；仍可能因法定合规要求审查输入输出。部署账号不得主动开启数据授权，页面必须提示不要上传个人信息或保密材料。

## 9. 自动更新规则

- 上传成功后立即显示该文件，并自动开始 AI 整理。
- AI 成功后直接更新画像，不需要任何确认。
- AI 只更新本次资料明确包含的卡片；未提及的卡片保持原样。
- 普通 AI 内容可以被更新资料覆盖。
- 管理者人工编辑后的卡片自动锁定，后续 AI 不得覆盖。
- 管理者可解除锁定，重新允许 AI 更新。
- 旧结果保留为隐藏历史，但老师首页只显示当前结果。
- 不展示置信度、候选、冲突矩阵或过期状态。
- AI 失败时文件仍保留，显示简短失败状态；只有管理者能点击“重新整理”。
- 每份文件只自动分析一次。

## 10. 权限边界

无需管理密钥：

- 查看学校画像
- 查看当前完成度
- 上传资料

需要现有管理链接：

- 编辑画像卡片
- 解除人工锁定
- 查看和下载原文件
- 删除资料
- 重新执行 AI 整理

原文件继续保存在私有 R2，不生成公开下载地址。普通老师看到“查看来源”时只显示来源文件名和更新时间，不直接下载原文件。

## 11. 最低防滥用

公开上传是付费 AI 接口，以下边界不得删除：

- Cloudflare Turnstile
- 同一网络地址每小时最多上传 5 份
- 每份文件只自动分析一次
- 火山方舟账号设置消费上限
- PDF、DOCX、XLSX 和 50MB 服务端边界校验
- 私有随机 R2 对象键
- D1 软删除
- 所有用户内容使用安全 DOM API 渲染

实现应尽量复用 `documents` 表记录上传频率和 AI 状态，避免为了限流单独搭建服务。不得保存明文 IP；如需计数，只保存服务端散列值并设置最短必要用途。

## 12. 最小数据模型方向

在确认远端从未应用 `0002` 后，优先改写尚未上线的画像迁移，而不是先创建 1.0 表再新增迁移删除它。

画像只需要：

- 八个固定 section key
- 当前内容
- 来源资料 ID
- 更新时间
- 是否人工锁定
- 历史版本

资料只需补充：

- AI 状态：未开始、处理中、完成、失败
- AI 错误的简短公开文案
- AI 分析时间
- 是否已自动分析
- 上传频率所需的不可逆网络标识

不要新增知识图谱、向量数据库、队列、工作流引擎、仓储层、服务层或前端框架。第一版使用“上传接口完成保存，页面自动调用一次分析接口”的两步 HTTP 流程；不增加消息队列。

## 13. 必须删除的 1.0 代码

- `PROFILE_DIMENSIONS`、36 个 `PROFILE_FIELDS`
- 权重、状态系数、有效期和冲突计算
- 候选创建、候选状态更新和候选删除流程
- 页面上的候选表单、字段长列表、维度卡片和状态矩阵
- 与双确认相关的 API、文案和测试
- 不再适用的 1.0 产品说明

可以复用的部分：

- 单个原生 `index.html`
- 现有鉴权和管理链接
- 文件边界校验
- D1、私有 R2、随机对象键
- 下载、软删除和安全 DOM 渲染
- 现有测试中的文件校验与存储假对象

## 14. 推荐实施顺序

1. 核对分支、工作区、远端和生产迁移状态。
2. 删除 1.0 的页面交互、共享常量、画像 API 和测试。
3. 将迁移改成八卡片与资料 AI 状态的最小模型。
4. 先实现不调用真实方舟的八卡片读取、人工编辑锁定和简单完成度。
5. 开放公开读取与上传；管理操作继续鉴权。
6. 加入 Turnstile、每小时 5 份限流和每文件一次分析。
7. 用原生 `fetch` 接入 Cloudflare `toMarkdown` 和火山方舟 Responses API。
8. AI 返回内容必须经过服务端 JSON、字段白名单和长度校验后才能写 D1。
9. 用模拟方舟响应完成自动更新、失败、重试、锁定不覆盖测试。
10. 运行所有 API 测试、Pages Functions 构建和本地完整闭环。
11. 用电脑端 Microsoft Edge 验收上传、AI 状态、卡片更新、管理编辑、响应式、焦点和控制台。
12. 检查并精简更新截图、`readme.md`、`handoff.md`、`AGENTS.md` 和本文；删除失效说明，准确内容不为制造差异而改写。
13. 除非用户明确要求不推送，只暂存本次文件，提交本地 `main`；重新核对远端后正常快进推送 `origin/main`。推送不代表授权生产部署或远端迁移。

## 15. 验收标准

- 零资料显示“已补全 0/8（0%）”。
- 老师无需密钥即可查看画像和上传。
- 上传表单只有文件、可选备注和上传按钮。
- 上传成功立即显示“AI 整理中”，不要求确认。
- 模拟 AI 返回后只更新有内容的卡片，完成度正确变化。
- AI 不清空未提及卡片，不覆盖人工锁定卡片。
- 管理链接可编辑、解锁、下载、删除和重试。
- 普通老师不能下载、编辑、删除或重试。
- Turnstile、5 份/小时、每文件一次分析有效。
- 错误格式、MIME、文件头和超过 50MB 仍被服务端拒绝。
- AI 失败不丢文件，页面能说明失败并允许管理员重试。
- 所有画像内容能看到来源文件名和更新时间。
- 不存在 36 字段、权重、候选或双确认界面。
- 无真实 API 密钥进入 Git、测试输出、截图或聊天。
- 不用真实生产资料做 AI 接口测试。

## 16. 发布边界

本次授权包括实现火山方舟数据流，但不包括在聊天中提供密钥，也不自动等于授权生产部署。

部署前需要用户在 Cloudflare 中自行配置或通过安全终端配置：

- `ARK_API_KEY`
- `TURNSTILE_SECRET`
- Turnstile Site Key（公开配置，不是秘密）
- Workers AI binding

测试阶段使用模拟方舟响应和无敏感信息的小型夹具。未获得明确发布授权前，不执行远端 D1 迁移、不部署 Pages、不向方舟发送生产资料。
