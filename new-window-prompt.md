# 学校画像碎片累积与线性撤销实施提示词

请接手 `D:\Project\ledu_project\Intelligence System` 的学校画像系统下一版优化，使用 `ponytail` full 模式实施已经确认的最小方案。

## 开始前

1. 完整阅读 `AGENTS.md`、`readme.md`、`handoff.md`、`school-profile-handoff.md`、本文件及相关代码。
2. 确认位于 `main`，工作区没有未知修改；只读核对 `origin/main` 与本地基线一致。遇到认证失败、冲突或未知远端变化立即停止。
3. 不读取、输出或记录任何 Secret、管理密钥、管理链接或原文件内容。
4. 当前生产仍是旧行为：AI 整卡替换、人工编辑/锁定、D1 软删除。本文件描述的碎片合并、线性撤销和彻底删除尚未实现。

## 已确认的产品行为

- 保持单校、八张画像卡片、三个首页区域和单个原生 `index.html`。
- 每张卡片显示当前综合画像，不显示碎片流水账。
- 每次 AI 整理输入“当前八张画像 + 新上传资料”。后上传资料优先：保留不冲突旧条目，替换冲突条目，删除过时条目并去重。
- AI 只返回新资料涉及卡片的完整新版；未涉及卡片保持不变。
- 卡片使用短条目，不设最低条数，最多 12 条，总长度不超过 4000 字。
- AI 结果与当前卡片完全相同时不写历史、不更新时间、不增加来源，也不产生可撤销步骤。
- 只有实际改变某卡片且尚未撤销的资料才出现在该卡片“参考资料”列表；同一资料影响多卡仍只算一次更新。
- 删除人工编辑、解除锁定、锁定提示、编辑弹窗、对应前端代码、画像 PATCH API 和 AI 跳过锁定逻辑。已上线的 `locked` 旧列保留但不再读取或写入，不为删除无害旧列重建生产表。
- 管理模式增加一个全局“撤销最近一次画像更新”。一次点击整体恢复最近一次资料更新前的所有受影响卡片和来源。
- 撤销是 Word 式线性栈：只能从最新一步向前撤销；无重做、分支、任意版本跳转或撤销后重新整理。撤销 B 后上传 C，B 不可重做，后续先撤销 C 再撤销 A。
- AI 失败仍允许管理员重试；已经成功后又撤销的资料不允许重新整理。
- 失败、未生效或已按顺序撤销的资料可以彻底删除。仍在有效撤销栈中的成功资料必须先按顺序撤销。
- 彻底删除移除私有 R2 对象、D1 `documents` 记录、文件名、对象键和来源身份；画像版本内容永久保留为匿名历史。

## ponytail full 技术边界

- 复用现有 `profile_sections`、`profile_history`、`documents` 和同一次分析共用的时间/文档身份。
- 新建 `0003` 迁移，只增加：
  - `documents.undone_at`
  - `profile_history.change_document_id`
  - `profile_history.previous_updated_at`
- 不新增 `profile_revisions`、`profile_section_sources` 或任何其他快照/来源表。
- `profile_history.change_document_id` 将同一资料造成的多卡更新组成一次撤销；更新前始终写入旧值，包括空内容。
- 迁移应利用现有 `profile_sections.source_document_id`、`updated_at` 和已有历史，安全补出当前生产状态的撤销起点；不得硬编码生产文档 ID、文件名或内容。
- 来源列表直接从当前卡片来源和仍有效、未撤销的历史变更推导，不重复存储。
- 方舟 function tool 最小改为每个 update 返回 `section_key` 和 `items` 数组；服务端验证 1–12 个非空字符串、八 key 白名单、去重和合计 4000 字上限，再用现有 `content` 文本列保存。不要引入 JSON 内容列或新渲染依赖。
- 撤销使用一个管理员 POST 接口，查找最新且仍有效、确实写过历史的资料，批量恢复其所有历史行并设置 `documents.undone_at`。不要实现重做。
- 删除继续使用现有管理员 DELETE 路径，但改为先验证删除资格，再删除 R2 对象、清除历史中的文件身份引用并硬删除 D1 文档记录。不要保留软删除分支。
- 保持 Turnstile、同网络散列每小时 5 份、PDF/DOCX/XLSX、50MB、扩展名/MIME/文件头校验、私有随机 R2 键、附件下载、无明文 IP、八 key 白名单和不可信资料提示词不变。

## 页面最小改动

- 删除编辑弹窗、编辑/解锁按钮、锁定标记和“人工内容优先”文案。
- 卡片内容渲染为语义化短条目列表；空卡片状态保持。
- “查看来源”显示该卡片当前有效的卡片级参考资料列表。
- 管理模式显示一个全局“撤销最近一次画像更新”，没有可撤销步骤时隐藏或禁用。
- 资料状态支持“更新已撤销”；失败资料仍显示“重新整理”，已撤销资料不显示重新整理。
- 删除确认文案必须明确原文件和记录会永久删除；不增加第二次确认。

## 必须覆盖的验证

- 更新 `tests/api.test.mjs` 和 `tests/profile.test.mjs`，删除人工编辑/锁定与软删除旧断言。
- 验证新资料合并当前画像、新资料优先、保留不冲突项、删除过时项、去重、最多 12 条和 4000 字限制。
- 验证相同结果不产生历史、来源或撤销步骤。
- 验证一份资料更新多卡只需一次撤销，撤销同时恢复内容、更新时间和来源。
- 验证 A、B 只能按 B 再 A 撤销；撤销 B 后上传 C 不得重做 B。
- 验证失败资料可重试，已撤销资料不可重新整理。
- 验证有效成功资料不能直接删除；失败、未生效或已撤销资料可彻底删除，R2 和 D1 均消失，匿名画像历史仍在且文件身份已清除。
- 验证撤销、下载和删除均需管理员密钥；公开查看、上传、Turnstile、限流和文件校验不得回退。
- 在全新本地 D1 顺序应用 `0001`、`0002`、`0003`，并完成对应本地 D1/R2/AI 模拟闭环。
- 运行：

```powershell
node --test tests/api.test.mjs
node --test tests/profile.test.mjs
npx.cmd wrangler pages functions build
```

- 用电脑端 Edge 或 Chrome 验收桌面端、390px、键盘焦点、撤销/删除主要流程和控制台错误，更新 `artifacts/school-archive-desktop.png`。

## Git 与发布

- 实现后精简更新 `AGENTS.md`、`readme.md`、`handoff.md`、`school-profile-handoff.md`，删除“尚未实现”说明，不追加流水账。
- 只暂存本次文件，提交本地 `main` 后再次只读核对 `origin/main`，仅正常快进推送，禁止强推。
- 推送代码不代表授权远端 D1 `0003` 迁移、生产部署、生产硬删除或真实生产 AI 调用；这些动作必须再次获得明确授权。
