# 网络资料生产状态

2026-09-09 已按 **ponytail full** 完成“小红书号”异步核验和浏览器自由选择上线。

## 当前生产

- Pages 部署 `4310e4b9`，源码提交 `6e2ff6f`，生产地址 <https://ledu-school-archive.pages.dev>。
- 远端 D1 已应用 `0001`～`0007`，无待应用迁移；原有账号、任务和结果完整保留，外键检查通过。
- `Ledu-Network-Materials-Worker` 使用提交 `6e2ff6f`，注册为每分钟触发、`MultipleInstances IgnoreNew`。人工登录已恢复，本地和 D1 停机标志均为关闭，任务状态为 `Ready`，恢复后的空闲轮询返回 0。
- `Ledu-Xiaohongshu-Course-Trial` 仍为 `Disabled`，旧 `seen.json`、状态与 `held_candidates` 未改动。工作器浏览器可显式指定或自动选择可用的本地 Edge、Chrome 或 Playwright Chromium。
- 既有 Secret 未修改；未调用真实 AI、未删除旧任务或历史候选，也未执行任何远端删除。

## 真实只读验收

- 核验账号：“杨老师的陪跑日记”，主页显示的小红书号 `9522680303`。
- 核验结果：`ready`，稳定主页 ID 已确认，错误为空，租约正常关闭。
- 搜索设置：1 个账号，关键词“学习”，近 7 日；每账号最多 20 条主页候选，每任务最多 30 条结果。
- 搜索结果：`completed / candidates_exhausted`；主页候选 20，窗口内图文 6，详情打开 6，关键词检查 6，命中与保存结果 5，计数完整。
- 验收结束后账号核验与检索活动任务均为 0，`network_worker_control.halted = 0`，本机停机标志为关闭。

## 已验证

- Node 测试 8 + 9 + 12 项、Python 测试 10 + 15 项全部通过。
- Pages Functions 构建成功；全新临时 D1 顺序应用 `0001`～`0007` 成功。
- GPT 内置浏览器已完成小红书号添加与状态展示、1280px、390px、主导航、键盘焦点和控制台验收；生产公开页面返回 200，新提示已生效，未登录 API 仍返回 401。
- 生产迁移记录、历史数据保留、外键完整性、部署源码、计划任务源码和空闲轮询均已只读复核。

## 后续边界

继续遵守 `AGENTS.md`。验证码、登录失效或安全验证必须 blocked、通知并停机，只能人工运行 `repair-login` 显式恢复。不得删除旧任务、历史候选或读取、输出 Secret、Cookie、管理链接、浏览器会话文件和原始凭据。

## 下一窗口：三个任务

三项工作在同一窗口内完成，并按 ponytail full 先追踪真实调用再做最小修改。

### 任务一：每位用户扫码绑定自己的小红书账号

在现有单页、Pages Functions、D1、本机串行工作器、当前 Conda 和 Windows 任务计划内完成以下能力，不增加新的常驻服务或前端框架：

- 每个已登录的内部用户都能在“网络资料”页发起小红书绑定。页面展示由工作器产生的短时扫码入口，并显示未绑定、等待扫码、已绑定、过期、失效和受安全验证阻断等明确状态；允许刷新二维码、解绑和重新绑定。
- 扫码成功后，本机工作器为该内部用户保存独立浏览器 profile。目录只使用不可逆或不可猜测的内部标识，不使用手机号、昵称或小红书号；不同内部用户的 Cookie、缓存和会话文件不得共用。
- 账号核验与网络检索必须按任务的 `user_id` 选择对应 profile。未绑定、会话失效或绑定被阻断时不得退回共享 profile，也不得使用其他用户会话。
- 前端浏览器与工作器浏览器是两层：用户可以用任意现代浏览器打开本站和查看二维码；扫码后得到登录状态的是本机工作器的该用户 profile，不能假设或读取用户访问本站浏览器里的小红书 Cookie。
- 小红书 Cookie、扫码 token、原始凭据和 profile 文件不得进入 D1、R2、日志、聊天或页面响应。若为跨进程展示二维码必须暂存挑战数据，先设计最小的短时、用户隔离方案，明确过期与删除流程，并确保页面只向发起绑定的有效会话返回；不得长期保存二维码或其可复用载荷。
- 保持全局任务串行、现有账号与检索配额、租约、访问预算、结果上限、历史数据和旧任务不变。需要新增绑定任务时，必须明确它与账号核验/检索的调度关系，避免一个等待扫码的用户无限占用全局队列。
- 禁用用户后不得再认领其绑定、核验或检索任务。解绑或重新绑定时只清理该用户对应的本机会话；不得删除其历史关注账号、搜索任务或结果。
- 验收覆盖至少两个内部用户，证明 profile 和结果不会串号；覆盖扫码过期、刷新、解绑、重新绑定、登录失效、安全阻断、任务排队以及旧共享 profile 的兼容迁移或明确退役路径。
- 页面与真实流程验收优先使用 GPT 内置浏览器，至少检查桌面、390px、键盘焦点、控制台和多种前端浏览器访问时的兼容性。工作器持久 profile 或扫码登录确实需要系统浏览器窗口时，可以使用任意可用本地浏览器。

先完整阅读 `readme.md`、`handoff.md`、`school-profile-handoff.md`、`AGENTS.md`、现有认证/网络 API、迁移和工作器代码，再给出最小设计并实现、测试和更新文档。代码验证通过后按仓库规则提交并正常快进推送；生产迁移、Pages 部署、计划任务切换和真实扫码验收仍须遵守 `AGENTS.md` 的单独授权边界。

### 任务二：消除仍可见的旧 24 位账号 ID 页面

用户在一个已登录窗口中看到“最多 3 个，仅接受 24 位标准账号 ID / 小红书账号 ID”，但 2026-09-09 已直接核对：当前 `main` 的 `index.html` 与标准生产域名 `https://ledu-school-archive.pages.dev/` 实时返回的 HTML 都是“填写个人主页昵称下方的小红书号”及 `red_id` 请求，不含 24 位限制。该任务必须先确认截图窗口的实际 URL、是否为旧 Pages 预览部署、其他域名、本地服务器、长时间未导航的旧文档或浏览器缓存，再只修真实根因。不要在已经正确的表单上叠加刷新脚本或新抽象。验收时证明所有正式入口都进入新表单，并记录旧入口的处理结果。

### 任务三：删除小红书部分冗余代码

完整追踪 `index.html` 的网络资料代码、`functions/api/network/`、`automation/xhs_course_trial.py`、`automation/network_worker.py`、任务注册脚本及相关测试的调用链。优先删除已失效的旧试运行入口、重复浏览器/登录/解析逻辑、无调用帮助函数、过期兼容分支和只验证实现细节的冗余测试；能复用现有函数时不新增抽象。不得凭名称猜测删除，旧计划任务、历史候选和生产数据保持不动。删减后必须运行最小而充分的测试与构建，并报告净代码行变化和保留下来的必要兼容路径。
