# 新窗口执行提示词

请接手当前仓库的“学校画像系统 2.0”简化重构任务，持续推进到可验证结果。产品方案已经确认，不要重新进行宽泛产品讨论。

项目目录：

`D:\Project\ledu_project\Intelligence System`

请使用 `ponytail` full 模式：删除优先、最少文件、零新前端框架、零不必要依赖和抽象。已经过度设计的 1.0 代码必须删除，不是在上面继续叠加。

开始前必须完整阅读：

- `AGENTS.md`
- `readme.md`
- `handoff.md`
- `school-profile-handoff.md`
- `profile-schema-v1.md`（只用于理解要删除的 1.0）
- 当前 `index.html`
- 全部 Pages Functions、D1 迁移和测试

然后确认：

1. 当前位于 `main`。
2. 工作区状态正常。
3. 本地与 `origin/main` 没有未知变化。
4. 生产仍未应用 `0002_create_profile_values.sql`；如无法确认，不得擅自改写或执行远端迁移。

遇到冲突、认证失败或未知远端变化立即停止。禁止强推。不得读取、输出、提交或在聊天中索要任何真实管理密钥、方舟密钥或 Turnstile Secret。

## 已确认的产品目标

把当前复杂的学校画像 1.0 改成公司内部教培老师使用的极简工具：

```text
老师打开网页
→ 立即看到一所学校的八张信息卡片
→ 拖入 PDF / DOCX / XLSX
→ 文件保存后自动 AI 整理
→ 画像直接更新
```

不需要资料确认、候选确认或任何确认状态。

## 已确认的页面

老师端只保留：

1. 学校信息概览
2. 极简上传
3. 最近更新

八张固定卡片：

1. 学校概况
2. 校历与作息
3. 年级与班级概况
4. 教材与当前教学进度
5. 考试安排与范围
6. 教学重点、难点与常见失分
7. 近期活动与通知
8. 可用教学资源

简单完成度：

```text
有内容的卡片数 ÷ 8
```

显示为“学校信息已补全 5/8（63%）”。不要字段权重、维度、候选、待确认、已确认、冲突、过期或有效期。

上传表单只保留：

- 文件
- 一句可选备注
- 上传按钮

文件名作为标题，分类、年级、学科和画像信息由 AI 识别。页面提示不得上传学生名单、手机号、个人成绩明细、身份证信息或公司保密资料。

## 已确认的权限

无需管理密钥：

- 查看画像
- 上传资料

需要现有管理链接：

- 编辑画像
- 解除人工锁定
- 下载来源
- 删除资料
- AI 失败后重新整理

原文件继续使用私有 R2 和随机对象键，不公开下载。普通老师只看到来源文件名和更新时间。

## 已确认的 AI 方案 B

```text
PDF / DOCX / XLSX
→ 私有 R2
→ Cloudflare Workers AI toMarkdown
→ 提取文字发送到火山方舟
→ Doubao-Seed-2.0-lite 返回画像更新
→ 服务端校验后写 D1
```

固定配置：

- Base URL：`https://ark.cn-beijing.volces.com/api/v3`
- Endpoint：`/responses`
- Model：`doubao-seed-2-0-lite-260215`
- Secret：`ARK_API_KEY`
- 原生 `fetch`，不要安装 SDK
- 一次函数调用返回最多八张卡片
- 关闭或最小化深度思考
- 不使用方舟知识库、向量库、Bot、文件索引或其他第三方解析服务

用户已授权将提取后的资料文字发送到火山方舟；原始文件不发送给方舟。测试不得使用生产资料，只能使用无敏感信息的小型夹具和模拟方舟响应。

## AI 更新规则

- AI 成功后直接写入画像，不要确认。
- 只更新本次资料明确包含的卡片。
- 未提及的卡片保持不变。
- AI 内容可被后续资料覆盖。
- 管理者人工编辑后自动锁定，AI 不得覆盖。
- 管理者可解除锁定。
- 旧值保留为隐藏历史，首页只显示当前值。
- AI 失败不丢文件；普通页面显示失败，管理员可以重试。
- 每份文件只自动分析一次。

## 最低防滥用

- Cloudflare Turnstile
- 同一网络地址每小时最多上传 5 份
- 每份文件只自动分析一次
- PDF、DOCX、XLSX 和 50MB 服务端校验
- 私有 R2、随机对象键、D1 软删除
- 不保存明文 IP
- 安全 DOM 渲染

不要用“简单易用”为理由删除服务端文件校验、费用防刷或私有存储。

## 必须删除的 1.0

- 36 个 `PROFILE_FIELDS`
- 七个维度和权重
- 状态系数、有效期和冲突计算
- 候选 API 与候选表单
- 双确认流程
- 字段长列表、状态矩阵和复杂缺失提示
- 对应测试和过时文档

可以复用：

- 单个原生 `index.html`
- Pages Functions、D1、私有 R2
- 现有鉴权和管理链接
- 文件签名/MIME/大小校验
- 下载、软删除和测试假对象

## 实施要求

1. 先给出简短执行计划，然后直接修改。
2. 优先改写尚未上线的 `0002`，不要故意创建 1.0 表再用新迁移删除；但必须先确认远端确实未应用。
3. 先完成八卡片、简单完成度、公开读取/上传和管理编辑锁定。
4. 再完成 Turnstile、5份/小时和每文件一次分析。
5. 最后接入 Cloudflare `toMarkdown` 与火山方舟；真实网络调用必须可被测试替身替换。
6. 方舟输出必须经过 JSON 解析、八个 section key 白名单和长度校验后才写 D1。
7. 不增加前端框架、构建体系、SDK、队列、知识库、向量库、工作流引擎或多学校支持。
8. 发现不再需要的 1.0 代码直接删除。

## 验证要求

- 运行现有及新增 API 测试。
- 验证零资料为 0/8。
- 验证公开查看和上传、管理操作鉴权。
- 验证 AI 只更新返回卡片、不清空其他卡片、不覆盖人工锁定。
- 验证失败保留文件、管理员可重试。
- 验证 Turnstile、每小时5份、每文件一次。
- 验证错误格式、MIME、文件头、超过50MB仍由服务端拒绝。
- 完成 Pages Functions 构建和本地真实闭环。
- 页面改动必须使用电脑端 Microsoft Edge 验收桌面、390px 响应式、键盘焦点和控制台错误。
- 更新 `artifacts/school-archive-desktop.png`、`readme.md`、`handoff.md` 和 `school-profile-handoff.md`。

## 每次任务的强制收尾

以后每次完成仓库任务，都要检查并按实际结果精简更新 `readme.md`、`handoff.md`、`AGENTS.md` 和相关专项交接文档：删除失效内容，不追加流水账；原内容仍准确时无需为了制造差异而重写。

除非用户明确要求不提交或不推送，验证通过后只暂存本次文件，提交到本地 `main`；再次只读核对远端后正常快进推送 `origin/main`。禁止强推。遇到冲突、认证失败或未知远端变化立即停止。推送代码不代表授权生产部署、远端 D1 迁移或使用真实生产资料调用 AI。

未得到新的明确授权前，不执行生产 D1 迁移、不部署 Pages、不调用真实方舟处理生产资料。部署所需 `ARK_API_KEY`、`TURNSTILE_SECRET` 和 Turnstile Site Key 只能由用户通过 Cloudflare 或安全终端配置，不能发到聊天。

请先用三到五句话复述你对“要删除什么、保留什么、最终老师流程”的理解，然后直接开始仓库核对和实施。除非发现会改变上述数据流、安全边界或单学校范围的事实，不要用宽泛问题阻塞推进。
